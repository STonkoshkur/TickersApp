/**
 * Measurements schema
 *
 * Used for elements spacing, UI sizes, roundness, etc.
 */
enum Measurements {
  zero = 0,
  tiny = 4,
  base = 8,
  medium = 12,
  double = 16,
  large = 24,
  huge = 30,
}

export default Measurements;
